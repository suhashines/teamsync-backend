meta {
  name: password-rest
  type: http
  seq: 8
}

post {
  url: {{BASE_SERVER}}/auth/password-reset
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{jwt}}
}

body:json {
  {
    "token": "string",
    "newPassword": "string"
  }
}
